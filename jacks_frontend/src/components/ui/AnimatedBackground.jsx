// Decorative blobs and food emoji particles removed — replaced with refined
// section-level design instead of global template animation noise.
export default function AnimatedBackground() {
  return null;
}
